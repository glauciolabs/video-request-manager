name: production workflow

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: write
  packages: write

on:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      pipeline_mode:
        description: Pipeline mode
        type: choice
        required: true
        default: build_and_push
        options:
          - build_and_push
          - build_only
          - deploy_only
      gitops_app:
        description: GitOps engine
        type: choice
        required: true
        default: argocd
        options:
          - argocd

jobs:
  pipeline:
    uses: glauciolabs/core-pipeline/.github/workflows/core-pipeline.yml@add2
    with:
      archetype: containers
      pipeline_mode: ${{ inputs.pipeline_mode || 'build_and_push' }}
      gitops_app: ${{ inputs.gitops_app || 'argocd' }}
      argocd_deployment_mode: kustomize
      argocd_application_manifest: ""
      release_policy: always
      release_notes_file: RELEASE_NOTES.md
      namespace_override: false
      kustomization_overlay: false
    secrets:
      REGISTRY: ghcr.io
      REGISTRY_USERNAME: ${{ github.repository_owner }}
      REGISTRY_PASSWORD: ${{ github.token }}
      GIT_REGISTRY_TOKEN: ${{ secrets.GIT_REGISTRY_TOKEN }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      ARGOCD_SERVER: ${{ secrets.ARGOCD_SERVER }}
      ARGOCD_TOKEN: ${{ secrets.ARGOCD_TOKEN }}
      GITOPS_SSH_PRIVATE_KEY: ${{ secrets.GITOPS_SSH_PRIVATE_KEY }}
      CLUSTER_DEVELOP: ${{ secrets.CLUSTER_DEVELOP }}
      CLUSTER_PRODUCTION: ${{ secrets.CLUSTER_PRODUCTION }}

  notify-telegram:
    name: Notify Telegram
    runs-on: ubuntu-latest
    needs: [pipeline]
    if: ${{ always() }}
    steps:
      - name: Send notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          WORKFLOW_NAME: ${{ github.workflow }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
          REF_NAME: ${{ github.ref_name }}
          STATUS: ${{ needs.pipeline.result }}
        run: |
          if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
            echo "Telegram secrets not set. Skipping notification."
            exit 0
          fi
          run_url="https://github.com/${REPO}/actions/runs/${RUN_ID}"
          text="Workflow: ${WORKFLOW_NAME}%0ARepo: ${REPO}%0ABranch: ${REF_NAME}%0AStatus: ${STATUS}%0ARun: ${run_url}"
          curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${text}" \
            -d "disable_web_page_preview=true" >/dev/null
